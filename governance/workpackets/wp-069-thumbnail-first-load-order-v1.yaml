workpacket_id: WP-069
taskboard_id: TB-FACIAL-WORKBENCH-001
title: Thumbnail-first layered load order and large-folder first-paint performance
status: in_progress
owner: model
created_at: "2026-08-12"
updated_at: "2026-08-12"
refinement: governance/refinements/wp-069-thumbnail-first-load-order-v1.md
dependencies: [WP-043, WP-050, WP-055, WP-056, WP-057, WP-064, WP-068]
scope:
  - product/src/ui.rs
  - product/src/media_explorer.rs
  - product/src/media_thumbs.rs
  - product/src/media_io.rs
  - product/src/media_search.rs
  - product/src/ui_inspect.rs
  - product/src/api.rs
  - product/docs/MANUAL.md
  - specs/app-spec.md
  - topology.yaml
  - governance/taskboard.yaml
progress:
  landed:
    - "Every ScanCacheReady and ScanDone publication now publishes an immediately renderable display order regardless of query or sort key; previously only the empty-query/Name-sort case did."
    - "ScanBatch appends every published batch to the display order, so the grid grows and stays scrollable during enumeration."
    - "start_compare_scan_internal no longer blanks a published order when a cached inventory is being reconciled."
    - "The canonical tile key is cached per path instead of being recomputed and allocated for every visible tile every frame, with a bounded cache."
  verified:
    - "cargo test: full suite green."
    - "Collection tabs publish rows and order in the same frame with no scan."
  outstanding:
    - "Measured time-to-first-tile / time-to-scrollable before and after on the operator's folders - the packet's primary acceptance surface. NOT measured; the improvement is currently structural, not evidenced."
    - "Explicit layer priority budgets and order-provenance diagnostics."
    - "Provisional-versus-ranked marking while a query is active."
    - "render_db_calls forbidden is still unenforced by test."
verification_round_2:
  - "Order provenance landed: media_tabs receipts report display_count and display_provenance (empty|provisional|settled). The grid renders the DISPLAY order, so a blank grid with a full inventory was previously invisible to diagnostics."
  - "MEASURED on a generated 20,000-file / 40-subfolder folder. With an active query mid-scan: display_count=20000, provenance=provisional, scanning=true — the grid is renderable and scrollable during the scan. Before this packet a non-empty query left the display order empty until the worker finished."
  - "After settle: display_count=4000, provenance=settled (query matched 4000 of 20000), so the provisional order is correctly replaced rather than left in place."
  - "Still outstanding: the operator's own 141,787-video mapped-drive folder, and per-layer priority budgets. Time-to-first-tile was measured by receipt polling, whose ~1.8s granularity is coarser than the value being measured."
base_scope:
  - "Publish a provisional, immediately renderable display order for every scan batch regardless of active query or sort key."
  - "Never blank an already-published display order; replace blanking with a generation-stamped swap."
  - "Layer work in an explicit priority order: row identity, visible thumbnails, overscan thumbnails, playback affordances, labels and favourite badges, then stat, search index, and semantic index."
  - "Decouple sort from first paint so a stat-dependent sort renders immediately and re-orders when the sweep completes."
  - "Remove avoidable per-frame allocation on the render path by caching the canonical key alongside each row."
  - "Make the render_db_calls forbidden rule enforceable by test instead of declarative."
high_roi_additions:
  - "Record measured time-to-first-row, time-to-first-thumbnail, time-to-scrollable, and time-to-settled before and after, so the improvement is evidence rather than assertion."
  - "Expose order provenance - cached, provisional, or settled - in scan and query diagnostics, reusing the WP-064 restore provenance field."
  - "Add a visible ordering indicator so re-ordering after a stat sweep is explained rather than surprising."
  - "Convert an unenforced topology claim into a tested invariant."
reuse:
  - "Existing 64-then-1024 progressive scan batching and the redb last-good inventory publication."
  - "Existing visible-plus-overscan thumbnail request loop, per-frame texture upload cap, and stale-job discard."
  - "Existing batched metadata hydration, which already avoids per-row database work."
  - "Existing media_io root-kind permits and work classes."
  - "Existing MediaDisplayCacheKey generations and the WP-064 per-tab display-order cache."
gaps_closed:
  - "The grid renders the display order, which is blanked at scan start and at tab materialization, so the grid is empty until a worker completes."
  - "The cheap identity-order publication applies only when the query is empty and sort is name, so any query or non-default sort turns a streaming folder open into a blank grid."
  - "A stat-dependent sort inserts a full metadata sweep between the operator and the first tile."
  - "The canonical key is recomputed and allocated per visible tile per frame."
  - "render_db_calls forbidden is declared in topology with no enforcement anywhere in code or tests."
acceptance_criteria:
  - "Opening a folder paints tiles from the first published batch and the grid is scrollable before enumeration completes, with any query and any sort key active."
  - "A published display order is never blanked; it is only swapped under a new generation stamp."
  - "Thumbnails for the visible band are requested before overscan, and playback affordances, labels, and favourite badges never gate row identity or thumbnails."
  - "A stat-dependent sort shows the provisional order immediately with a visible ordering indicator and re-orders when the sweep completes."
  - "Actions resolve through the canonical key, never the display index, and destructive actions are gated on a settled generation."
  - "A provisional order is rejected rather than rendered when its generations or tab identity do not match."
  - "While a query is active, a provisional order is either clearly marked unranked or the prior ranked order is retained; a wrong ranking is never shown silently."
  - "Zero storage transactions occur during a rendered frame, asserted by test."
  - "Measured time-to-first-tile and time-to-scrollable improve on the operator's local folder and the 141,787-video mapped-drive folder, with before and after numbers recorded."
  - "Playback priority still preempts thumbnail work, and all layers eventually complete on a remote root."
  - "Frame time during scroll on the large folder shows no regression against the current build."
risks:
  - scenario: "A provisional order lets the operator act on a row that the settled order moves or removes."
    mitigation: "Actions resolve through the canonical key and destructive actions are gated on a settled generation."
  - scenario: "Visible reordering jitter as batches land."
    mitigation: "Append-only publication during enumeration for the default order, a single settle transition, and a visible provisional indicator."
  - scenario: "A provisional order is mistaken for a ranked search result."
    mitigation: "Mark provisional orders unranked while a query is active, or retain the prior ranked order."
  - scenario: "Removing blanking causes cross-tab or cross-scan bleed."
    mitigation: "Carry full display-cache generations plus tab identity and reject mismatches."
  - scenario: "Lower layers starve indefinitely on a busy remote root."
    mitigation: "Per-layer permit budgets with a floor, and an assertion that all layers complete on the operator's mapped-drive folder."
  - scenario: "Playback priority regresses under the new layering."
    mitigation: "Re-run the existing playback-priority assertions with layering enabled."
  - scenario: "Performance is claimed without measurement."
    mitigation: "Acceptance is measured time-to-first-tile and time-to-scrollable on the operator's own folders, recorded before and after."
validation:
  - method: "instrumented measurement on the operator's local folder and the 141,787-video recursive mapped-drive folder"
    expected: "Time to first row, first thumbnail, scrollable, and settled order are recorded before and after, and show improvement."
  - method: "the same measurements repeated for each WP-068 sort key"
    expected: "No sort key blanks the grid."
  - method: "cargo test --manifest-path product/Cargo.toml"
    expected: "All tests pass, including provisional publication for every batch, never-blank swap, order rejection on mismatch, layer priority and cancellation, and zero render-path storage transactions."
  - method: "facial-cli ui-inspect presets for a mid-scan provisional grid, a settled grid, and a stat-sorted grid"
    expected: "Affected PNG and layout JSON are directly inspected and legible."
  - method: "scan and query diagnostics against facial.exe --background"
    expected: "Published row count, order provenance, and per-layer progress are reported without foreground activation."
  - method: "frame-time sampling during scroll on the large folder"
    expected: "No regression against the current build."
  - method: "high-risk adversarial review"
    expected: "No unresolved order-provenance, cross-tab attribution, starvation, or action-resolution findings remain."

render_path_invariant:
  claim: "Zero storage transactions occur during a rendered frame."
  status: enforced
  mechanism: "MediaDb owns an AtomicU64 shared with MediaInventoryStore; every begin_write/begin_read goes through CountedTransactions::counted_write/counted_read (25 call sites across both databases), so no transaction path is uncounted."
  probe: "App::debug_media_transaction_count exposes the total to the headless inspector."
  gate: "facial-cli ui-inspect preset media_render_txn_free samples the count, renders 3 settled frames after 3 warm-up frames, and fails the whole run on any delta."
  observed_pass: "count stayed 26 across the measured frames; 26 is non-zero, proving the counter is wired to real transactions rather than reading a dead field."
  negative_test: "A temporary self.media_db.color_label_definitions() call at the top of render_ui made the gate fail with 'media render path opened 3 storage transaction(s) across 3 settled frames (count 1690 -> 1693)'. The probe was then removed and the gate returned to passing. The guard is therefore proven to catch the defect class, not merely to pass."
  topology_link: "topology.yaml media.render_db_calls: forbidden — previously an honour-system declaration with no enforcement; now carries render_db_calls_enforcement naming this gate."

layer_priority_budgets:
  defect: "The stat sweep behind a size/date sort and the semantic index query both enqueued as WorkClass::Metadata (priority 2), which outranks WorkClass::Prefetch (priority 4). Opening a large folder with a size sort therefore let a whole-folder stat sweep take I/O permits ahead of overscan thumbnail work -- starving the thumbnails the sort exists to reorder, on exactly the large mapped-drive folders the app was built for."
  change: "Added WorkClass::Background (priority 5, lowest) for whole-folder sweeps no visible row is waiting on, and reclassified both call sites in ui.rs. Effective layer order is now Playback > Visible > Scan (row identity) > Prefetch (overscan thumbnails) > Background (stat sweep, semantic index)."
  starvation_floor: "Background inherits the existing IoPolicy::priority_burst fairness turn, so it is throttled, not starved -- a size sort still settles."
  metadata_class: "WorkClass::Metadata now has no producers. It is retained and documented as the reserved slot between visible thumbnails and enumeration; per-row hydration is served from the in-memory cache and never touches the filesystem."
  test: "media_io::tests::whole_folder_sweeps_yield_to_thumbnail_prefetch_but_still_get_a_turn -- the sweep queues FIRST, so only priority can put the later prefetch ahead of it, and the sweep must still be granted after the burst cap."
  negative_test: "Reclassifying the sweeps back to WorkClass::Metadata makes that test FAIL. The test discriminates the defect rather than merely passing."
  measurement_status: "This is a proven ordering change, NOT a measured latency improvement. Time-to-first-thumbnail under a size sort on the operator's 141,787-video mapped drive is still unmeasured -- that folder cannot be synthesized here."

startup_measurement:
  method: "facial.exe --background started cold, then a media_tabs list intent issued immediately; the clock stops when that intent's receipt leaves 'accepted', i.e. the live GUI has reached its intent-apply loop and is interactive. 3 runs each."
  release_build_ms: [4441, 4163, 4731]
  release_median_ms: 4441
  debug_build_ms: [18261, 20725, 16978]
  overhead_caveat: "The measured figure includes up to ~1000 ms of intent-poll latency (the idle repaint/poll cadence is 1000 ms) plus 100 ms of polling granularity in the harness, so true time-to-interactive is roughly 3.2-3.7 s. The number is an upper bound, not a floor."
  interpretation: "This is a baseline, not an improvement claim. No before/after startup comparison was made, because the packet's load-order work targets folder open latency rather than process start."
  environment: "Windows 11, this workstation, workspace root D:/Projects/LLM projects/comfyui-workbench/projects/leeseo. Numbers are not portable to other machines."
